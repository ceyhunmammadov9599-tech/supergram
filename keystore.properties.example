# SuperGram release signing configuration.
# Copy this file to keystore.properties (git-ignored) and fill in your values
# after generating a keystore with scripts/generate-release-keystore.sh.
# The release build then automatically signs the APK.
storeFile=supergram-release.keystore
storePassword=CHANGE_ME
keyAlias=supergram
keyPassword=CHANGE_ME
